{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "cd frontend-popayan && npm install && npm run build && rm -rf ../public/assets && cp -rf dist/* ../public/ && cp -f dist/index.html ../resources/views/welcome.blade.php"
    },
    "devDependencies": {
        "axios": "^1.13.5",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}